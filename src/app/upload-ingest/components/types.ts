export interface IngestFormData {
  imageFile: FileList | null;
  source: string;
  postTimestamp: string;
  caption: string;
  postId: string;
  examId: string;
  examDate: string;
  centreId: string;
  paperCode: string;
  setNumber: string;
}
